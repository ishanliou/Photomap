<h1>Users#index</h1>
<p>Find me in app/views/users/index.html.erb</p>

<h1>Users list</h1>
<ul>
    
        <% @users.each do |u|%>
            <li> <%= link_to(u.name, user_path(u))%> </li>
        <% end %>

</ul>
